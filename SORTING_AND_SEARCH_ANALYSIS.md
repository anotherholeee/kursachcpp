# Анализ сортировок и поиска в коде

## 🔍 ПОИСК (Search)

### 1. Алгоритмы поиска маршрутов

#### 1.1. BFS (Поиск в ширину) - `algorithm.cpp`

**Местоположение:** `algorithm.cpp:7-105`

**Реализация:**
```cpp
List<Journey> BFSAlgorithm::findPath(const std::string& start,
                                     const std::string& end,
                                     const Time& departureTime)
```

**Как работает:**
- Использует очередь (`std::queue<SearchNode>`) для обхода графа остановок
- Начинает с начальной остановки (строка 25)
- Для каждой остановки получает все проходящие рейсы (строка 44)
- Проверяет все последующие остановки на каждом рейсе (строки 71-95)
- Сохраняет найденные маршруты при достижении конечной остановки (строки 32-35)
- Ограничивает количество пересадок (строка 39)

**Использование:**
- Вызывается из `JourneyPlanner::findJourneysWithTransfers()` (journey_planner.cpp:29)
- Используется внутри `FastestPathAlgorithm` (algorithm.cpp:112)
- Используется внутри `MinimalTransfersAlgorithm` (algorithm.cpp:130)

#### 1.2. Поиск самого быстрого маршрута - `algorithm.cpp`

**Местоположение:** `algorithm.cpp:107-123`

**Реализация:**
```cpp
List<Journey> FastestPathAlgorithm::findPath(...)
```

**Как работает:**
- Использует BFS для поиска всех маршрутов
- Выбирает первый маршрут из отсортированного списка (строка 121)
- BFS уже отсортировал маршруты по времени в пути

**Использование:**
- Вызывается из `JourneyPlanner::findFastestJourney()` (journey_planner.cpp:185)

#### 1.3. Поиск маршрута с минимальными пересадками - `algorithm.cpp`

**Местоположение:** `algorithm.cpp:125-146`

**Реализация:**
```cpp
List<Journey> MinimalTransfersAlgorithm::findPath(...)
```

**Как работает:**
- Использует BFS для поиска всех маршрутов
- Использует `std::min_element` для поиска маршрута с минимальным количеством пересадок (строки 138-141)

**Использование:**
- Вызывается из `JourneyPlanner::findJourneyWithLeastTransfers()` (journey_planner.cpp:197)

#### 1.4. Поиск всех маршрутов (без привязки ко времени) - `journey_planner.cpp`

**Местоположение:** `journey_planner.cpp:37-180`

**Реализация:**
```cpp
List<Journey> JourneyPlanner::findAllJourneysWithTransfers(...)
```

**Как работает:**
- Использует BFS с модификациями
- Начинает со всех рейсов через начальную остановку (строки 55-68)
- Использует `std::set` для отслеживания посещенных узлов (строки 73, 104-108)
- Ограничивает количество итераций для предотвращения зависания (строки 75-76, 85)

**Использование:**
- Вызывается из Qt UI при поиске маршрутов (qt_ui.cpp:1428)

### 2. Поиск маршрутов между остановками - `algorithm.cpp`

**Местоположение:** `algorithm.cpp:193-210`

**Реализация:**
```cpp
List<std::shared_ptr<Route>> RouteSearchAlgorithm::findRoutes(
    const std::string& stopA, const std::string& stopB)
```

**Как работает:**
- Перебирает все маршруты в системе (строка 201)
- Проверяет, содержит ли маршрут обе остановки (строки 203-204)
- Проверяет, что stopA идет раньше stopB (строка 205)

**Использование:**
- Вызывается из `TransportSystem::findRoutes()` (transport_system.cpp:84-86)
- Используется в Qt UI для поиска маршрутов (qt_ui.cpp:1393)

### 3. Поиск элементов в контейнерах

#### 3.1. Поиск водителя по имени - `transport_system.cpp`

**Местоположение:** `transport_system.cpp:337-348`

**Реализация:**
```cpp
std::shared_ptr<Driver> TransportSystem::findDriverByName(
    const std::string& firstName,
    const std::string& lastName,
    const std::string& middleName) const
```

**Как работает:**
- Линейный поиск по списку водителей (строки 340-346)
- Сравнивает имя, фамилию и отчество (если указано)

**Использование:**
- Используется при загрузке рейсов из файла (trip.cpp:298, 306, 317)
- Используется в Qt UI (qt_ui.cpp:767)

#### 3.2. Поиск транспортного средства по номеру - `transport_system.cpp`

**Местоположение:** `transport_system.cpp:350-358`

**Реализация:**
```cpp
std::shared_ptr<Vehicle> TransportSystem::findVehicleByLicensePlate(
    const std::string& licensePlate) const
```

**Как работает:**
- Линейный поиск по списку транспортных средств (строки 352-356)
- Сравнивает государственный номер

**Использование:**
- Используется при загрузке рейсов из файла (trip.cpp:236, 283)

#### 3.3. Поиск маршрута по номеру - `transport_system.cpp`

**Местоположение:** `transport_system.cpp:360-368`

**Реализация:**
```cpp
std::shared_ptr<Route> TransportSystem::findRouteByNumber(int number) const
```

**Как работает:**
- Линейный поиск по списку маршрутов (строки 362-366)
- Сравнивает номер маршрута

**Использование:**
- Используется при загрузке данных из файлов

### 4. Поиск с использованием STL алгоритмов

#### 4.1. `std::find_if` - поиск с предикатом

**Использование:**

1. **Поиск рейса по ID** - `algorithm.cpp:159`
   ```cpp
   auto tripIt = std::find_if(trips.begin(), trips.end(),
                              [tripId](const auto& t) { 
                                  return t->getTripId() == tripId; 
                              });
   ```

2. **Поиск маршрута по номеру** - `transport_system.cpp:252`
   ```cpp
   auto it = std::find_if(routes.begin(), routes.end(),
                         [routeNumber](const auto& r) { 
                             return r->getNumber() == routeNumber; 
                         });
   ```

3. **Поиск рейса по ID** - `transport_system.cpp:263`
   ```cpp
   auto it = std::find_if(trips.begin(), trips.end(),
                         [tripId](const auto& t) { 
                             return t->getTripId() == tripId; 
                         });
   ```

4. **Поиск остановки в маршруте** - `route.cpp:20`
   ```cpp
   return std::find(allStops.begin(), allStops.end(), stop) != allStops.end();
   ```

5. **Поиск в Qt UI** - `qt_ui.cpp:1287`
   ```cpp
   auto tripIt = std::find_if(trips.begin(), trips.end(), ...);
   ```

#### 4.2. `std::min_element` - поиск минимального элемента

**Использование:**

1. **Поиск маршрута с минимальными пересадками** - `algorithm.cpp:138`
   ```cpp
   auto it = std::min_element(journeys.begin(), journeys.end(),
                             [](const Journey& a, const Journey& b) {
                                 return a.getTransferCount() < b.getTransferCount();
                             });
   ```

#### 4.3. Поиск в map/set

**Использование:**

1. **Поиск остановки по ID** - `transport_system.cpp:94`
   ```cpp
   auto it = stopIdToName.find(stopId);
   ```

2. **Поиск дня недели в маршруте** - `route.cpp:76`
   ```cpp
   return weekDays.find(day) != weekDays.end();
   ```

3. **Поиск администратора** - `transport_system.cpp:56`
   ```cpp
   auto it = adminCredentials.find(username);
   ```

4. **Проверка посещенных узлов** - `journey_planner.cpp:105`
   ```cpp
   if (visited.find(visitKey) != visited.end())
   ```

5. **Поиск остановки в расписании рейса** - `trip.cpp:28, 37`
   ```cpp
   auto it = schedule.find(stop);
   return schedule.find(stop) != schedule.end();
   ```

---

## 📊 СОРТИРОВКА (Sorting)

### 1. Реализация метода sort для List - `list.tpp`

**Местоположение:** `list.tpp:196-216`

**Реализация:**
```cpp
template<typename T>
template<typename Compare>
void List<T>::sort(Compare comp)
```

**Как работает:**
1. Если список содержит 1 элемент или меньше - выходит (строка 197)
2. Копирует элементы в `std::list` (строки 200-205)
3. Использует `std::list::sort()` для сортировки (строка 208)
4. Очищает текущий список (строка 211)
5. Копирует отсортированные элементы обратно (строки 214-215)

**Объявление:** `list.h:139-140`

### 2. Использование сортировки в коде

#### 2.1. Сортировка маршрутов по времени в пути - `algorithm.cpp`

**Местоположение:** `algorithm.cpp:99-102`

```cpp
// Сортируем найденные маршруты по времени в пути (от быстрых к медленным)
journeys.sort([](const Journey& a, const Journey& b) {
    return a.getTotalDuration() < b.getTotalDuration();
});
```

**Использование:**
- В `BFSAlgorithm::findPath()` после поиска всех маршрутов
- Результат используется в `FastestPathAlgorithm` (берется первый элемент)

#### 2.2. Сортировка маршрутов по времени отправления и длительности - `journey_planner.cpp`

**Местоположение:** `journey_planner.cpp:171-177`

```cpp
// Сортируем найденные маршруты: сначала по времени отправления, затем по длительности
journeys.sort([](const Journey& a, const Journey& b) {
    if (a.getStartTime() != b.getStartTime()) {
        return a.getStartTime() < b.getStartTime();
    }
    return a.getTotalDuration() < b.getTotalDuration();
});
```

**Использование:**
- В `JourneyPlanner::findAllJourneysWithTransfers()` после поиска всех маршрутов

#### 2.3. Сортировка рейсов по времени прибытия на начальную остановку - `journey_planner.cpp`

**Местоположение:** `journey_planner.cpp:65-68`

```cpp
// Сортируем рейсы по времени прибытия на начальную остановку
validInitialTrips.sort([&startStop](const auto& a, const auto& b) {
    return a->getArrivalTime(startStop) < b->getArrivalTime(startStop);
});
```

**Использование:**
- В `JourneyPlanner::findAllJourneysWithTransfers()` перед началом поиска

#### 2.4. Сортировка рейсов по времени прибытия - `transport_system.cpp`

**Местоположение:** `transport_system.cpp:113-114`

```cpp
// Сортируем рейсы по времени прибытия
relevantTrips.sort([](const auto& a, const auto& b) { 
    return a.second < b.second; 
});
```

**Использование:**
- В `TransportSystem::getStopTimetable()` для вывода расписания остановки

**Другое использование:** `transport_system.cpp:138-139`
```cpp
// Сортируем используя метод sort
relevantTrips.sort([](const auto& a, const auto& b) { 
    return a.second < b.second; 
});
```
- В `TransportSystem::getStopTimetableAll()` для вывода полного расписания

#### 2.5. Сортировка рейсов по дню недели и времени - `qt_ui.cpp`

**Местоположение:** `qt_ui.cpp:502-509`

```cpp
// Сортируем по дню недели, затем по времени начала
validTripsList.sort([](const std::shared_ptr<Trip>& a, const std::shared_ptr<Trip>& b) {
    int dayA = a->getRoute()->getWeekDays().size() > 0 
               ? *a->getRoute()->getWeekDays().begin() : 0;
    int dayB = b->getRoute()->getWeekDays().size() > 0 
               ? *b->getRoute()->getWeekDays().begin() : 0;
    if (dayA != dayB) return dayA < dayB;
    // Если день одинаковый, сортируем по времени начала
    return a->getStartTime() < b->getStartTime();
});
```

**Использование:**
- В Qt UI для отображения рейсов в удобном порядке

#### 2.6. Сортировка расписания по времени - `qt_ui.cpp`

**Местоположение:** `qt_ui.cpp:1091-1093`

```cpp
List<std::pair<std::string, Time>> sortedSchedule(schedule.begin(), schedule.end());
sortedSchedule.sort([](const auto& a, const auto& b) { 
    return a.second < b.second; 
});
for (const auto& [stop, time] : sortedSchedule) {
```

**Использование:**
- В Qt UI для отображения расписания рейса по времени прибытия

#### 2.7. Сортировка релевантных рейсов - `qt_ui.cpp`

**Местоположение:** `qt_ui.cpp:1266`

```cpp
relevantTrips.sort([](const auto& a, const auto& b) { 
    return a.second.second < b.second.second; 
});
```

**Использование:**
- В Qt UI для сортировки результатов поиска

#### 2.8. Сортировка времени остановок - `ui.cpp`

**Местоположение:** `ui.cpp:914-915`

```cpp
// Сортируем используя метод sort
stopTimes.sort([](const auto& a, const auto& b) { 
    return a.second < b.second; 
});
```

**Использование:**
- В консольном UI для вывода расписания остановки

---

## 📋 Сводная таблица

### Поиск

| Алгоритм/Метод | Файл | Строки | Тип поиска |
|----------------|------|--------|------------|
| BFSAlgorithm::findPath | algorithm.cpp | 9-105 | Поиск в ширину |
| FastestPathAlgorithm::findPath | algorithm.cpp | 109-123 | Выбор из отсортированного |
| MinimalTransfersAlgorithm::findPath | algorithm.cpp | 127-146 | Поиск минимума |
| JourneyPlanner::findAllJourneysWithTransfers | journey_planner.cpp | 37-180 | BFS с модификациями |
| RouteSearchAlgorithm::findRoutes | algorithm.cpp | 193-210 | Линейный поиск |
| TransportSystem::findDriverByName | transport_system.cpp | 337-348 | Линейный поиск |
| TransportSystem::findVehicleByLicensePlate | transport_system.cpp | 350-358 | Линейный поиск |
| TransportSystem::findRouteByNumber | transport_system.cpp | 360-368 | Линейный поиск |
| std::find_if | Множество файлов | - | STL алгоритм |
| std::min_element | algorithm.cpp | 138 | STL алгоритм |
| map/set::find | Множество файлов | - | Бинарный поиск |

### Сортировка

| Место использования | Файл | Строки | Критерий сортировки |
|---------------------|------|--------|---------------------|
| BFSAlgorithm::findPath | algorithm.cpp | 100-102 | Время в пути |
| JourneyPlanner::findAllJourneysWithTransfers | journey_planner.cpp | 172-177 | Время отправления, затем длительность |
| JourneyPlanner::findAllJourneysWithTransfers | journey_planner.cpp | 66-68 | Время прибытия на начальную остановку |
| TransportSystem::getStopTimetable | transport_system.cpp | 114 | Время прибытия |
| TransportSystem::getStopTimetableAll | transport_system.cpp | 139 | Время прибытия |
| Qt UI - отображение рейсов | qt_ui.cpp | 503-509 | День недели, затем время начала |
| Qt UI - расписание рейса | qt_ui.cpp | 1092 | Время прибытия |
| Qt UI - результаты поиска | qt_ui.cpp | 1266 | Время прибытия |
| UI - расписание остановки | ui.cpp | 915 | Время прибытия |

---

## 🔑 Ключевые особенности

### Поиск:
1. **BFS** - основной алгоритм поиска маршрутов с пересадками
2. **Линейный поиск** - используется для поиска элементов в списках (водители, транспорт, маршруты)
3. **STL алгоритмы** - `std::find_if`, `std::min_element` для эффективного поиска
4. **Ассоциативные контейнеры** - `map` и `set` для быстрого поиска по ключу

### Сортировка:
1. **Реализация через std::list** - метод `List::sort()` использует `std::list::sort()` внутри
2. **Лямбда-функции** - все сортировки используют лямбда-функции как компараторы
3. **Множественные критерии** - некоторые сортировки используют несколько критериев (время отправления + длительность)
4. **Сортировка по времени** - наиболее часто используемый критерий (время прибытия, время отправления, время в пути)




